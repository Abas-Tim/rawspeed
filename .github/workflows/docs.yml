name: Docs building

on:
  schedule:
    - cron: "0 0 * * SUN"
  workflow_dispatch:


# Sets permissions of the GITHUB_TOKEN to allow deployment to GitHub Pages
permissions:
  contents: read
  pages: write
  id-token: write

# Allow one concurrent deployment
concurrency:
  group: "pages"
  cancel-in-progress: true

# Default to bash
defaults:
  run:
    shell: bash

jobs:
  # Build job
  build:
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: Install deps
        run: |
          sudo apt-get -y install \
            intltool \
            libatk1.0-dev \
            libjpeg-dev \
            libjson-glib-dev \
            libpugixml-dev \
            libxml2-dev \
            libxml2-utils \
            python3-jsonschema \
            xsltproc \
            doxygen \
            graphviz \
            python3-sphinx \
            python3-sphinx-rtd-theme;
      - name: Checkout
        uses: actions/checkout@v4
        with:
          submodules: recursive
          fetch-depth: 0
      - name: Minimal project build with docs
        env:
          SRC_DIR: ${{ github.workspace }}
          BUILD_DIR: ${{ github.workspace }}/build
          ECO: -DBUILD_DOCS=ON -DBUILD_BENCHMARKING=OFF -DBUILD_TESTING=OFF
        run: |
          mkdir $BUILD_DIR
          cd $BUILD_DIR
          cmake ${ECO} "$SRC_DIR"
          cmake --build "$BUILD_DIR" -t sphinx doxygen
      - name: Upload page artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: ./build/docs/html
      - name: Setup Pages
        id: pages
        uses: actions/configure-pages@v2
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
